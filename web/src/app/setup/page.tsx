import { SetupForm } from "@/auth/components/setup-form";

export default function SetupPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-50 p-4">
      <SetupForm />
    </div>
  );
}
